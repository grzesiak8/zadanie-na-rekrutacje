{% extends 'base.html.twig' %}

{% block body %}
    <div id="wrapper">
        <div id="container">
            {{ form(form) }}
            {% if solution %}
                {{ solution }}
            {% endif %}
        </div>
    </div>
{% endblock %}